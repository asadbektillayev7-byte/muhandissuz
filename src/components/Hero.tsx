import HeroRotator from "@/components/HeroRotator";

const Hero = () => {
  return (
    <section className="relative w-full overflow-hidden">
      <HeroRotator />
    </section>
  );
};

export default Hero;
